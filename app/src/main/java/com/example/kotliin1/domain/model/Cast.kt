package com.example.kotliin1.domain.model

data class Cast(
    val name: String,
    val character: String,
    val profilePath: String?,
    val id: Int
)
// Wrapper for cast response (adjust if needed)
